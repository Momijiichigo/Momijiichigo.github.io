/*const ghCanvas = document.querySelector('#gestureHandler'); //ghCanvas: gesture handler canvas
const onResize = ()=>{
  ghCanvas.width = document.body.clientWidth;
  ghCanvas.height = document.body.clientHeight;
}
onResize();
window.addEventListener('resize',e=>{
  onResize();
})*/
const gh = document.querySelector('#gestureHandler'); //ghCanvas: gesture handler canvas
//gh.onclick = () =>console.log('gh clicked')